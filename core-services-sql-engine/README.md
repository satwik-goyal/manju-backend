# SQL Digestion Engine

Day 1-3: SQL Digestion Engine core
- Docker compose with platform DB + test source DB
- PostgreSQL connector (discover + pull)
- Engine library (register source, discover, sync, change detection)
- Echo API endpoints for sources, discovery, sync, data query, changes
- Background scheduler for automated syncs
- Test with seeded sample database

